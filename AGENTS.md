# Faberon agent instructions

Read `CONSTITUTION.md` before changing this repository.

The application lives in `frontend`. Use the project skill at `.cursor/skills/build-faberon-frontend` for implementation and `.cursor/skills/review-faberon-frontend` for reviews. Run package commands from `frontend` with Node 26 and pnpm.
