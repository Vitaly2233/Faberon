/** Integer minor units, e.g. grosze for PLN. */
export type MoneyMinor = number;

/** Decimal string representation for API payloads when minor units are not used. */
export type MoneyString = string;
